// Private route – minimal placeholder
export default async function Dashboard(container) {
    container.innerHTML = `
      <div class="card">
        <h2 style="margin:0 0 1rem 0;">Dashboard</h2>
        <p>Welcome! Auth & routing are working. Next blocks: Generator, Products, Calendar.</p>
      </div>
    `;
  }
  